# Deploy 

Run the following command to build the project:`

```bash
npm run generate
```

To re-authenticate with Firebase, use the command:

```bash
firebase login --reauth
```

Then, deploy the project with:

```bash
npm run deploy
```
